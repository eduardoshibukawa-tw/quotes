spring:
  application:
    name: julius

  cloud:
    config:
      uri: http://localhost:8888
      name: ${spring.application.name}, settings, toggles
    bus:
      enabled: true
      refresh:
        enabled: true


  rabbitmq:
    host: localhost
    port: 5672
    username: springcloud
    password: 123456